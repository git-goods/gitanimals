query {
    rateLimit {
        limit
        cost
        remaining
        resetAt
        used
    }
    user(login: "*{name}") {
        contributionsCollection {
            contributionYears
        }
    }
}
